// The test files import from "bun:test". Referencing bun-types here keeps
// `tsc --noEmit` happy without adding "types" to tsconfig, which would switch
// off the automatic @types/* discovery that Next relies on.
/// <reference types="bun-types" />
